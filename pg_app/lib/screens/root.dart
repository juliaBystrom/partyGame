import 'package:flutter/material.dart';
import 'package:pg_app/screens/Login/login.dart';

class Root extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Login(),
    );
  }
}